# GM-030/GM-040 Streamlit Community Cloud runtime requirements.
# The editable self-install line makes the host install the local project
# itself (GM-040-HF1), so `importlib.metadata.version("greenmachine")`
# resolves on the deployed environment exactly as it does locally. The four
# bounded specifications below are the project's runtime dependencies
# (pyproject.toml [project].dependencies) plus the UI dependency (the [ui]
# extra). Development-only tools (pytest, hypothesis, ruff, mypy, pre-commit)
# are deliberately absent: they install via `pip install -e ".[dev,ui]"` for
# local work, never on the host.
-e .
streamlit>=1.32,<2
PyYAML>=6,<7
pydantic>=2,<3
tzdata>=2024.1
