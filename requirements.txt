# GM-030/GM-040 Streamlit Community Cloud runtime requirements.
# The editable self-install line makes the host install the local project
# itself (GM-040-HF1), so `importlib.metadata.version("greenmachine")`
# resolves on the deployed environment exactly as it does locally. The five
# bounded specifications below are the project's runtime dependencies
# (pyproject.toml [project].dependencies) plus the UI dependencies (the [ui]
# extra — streamlit floored at 1.37 per D-062, <2 as the major-version
# boundary; pandas declared directly for the grid's Styler grading, D-059).
# Development-only tools (pytest, hypothesis, ruff, mypy, pre-commit) are
# deliberately absent: they install via `pip install -e ".[dev,ui]"` for
# local work, never on the host.
# D-137 (2026-08-26): cache-bust marker. Streamlit Community Cloud reuses its
# cached install environment while this file is unchanged; after D-136's
# deploy the app served an ImportError crash page while the identical code
# imported cleanly everywhere else, so this comment changes the file and
# forces the host to rebuild its environment from scratch on the next deploy.
# D-149 (2026-08-31): bumped again — after D-148's deploy the app served a
# TypeError crash page from inside the board build while the identical
# commit built cleanly off-host on Python 3.12 AND on a fresh 3.14
# environment (the host's interpreter family): the same stale-environment
# failure mode as D-137, so the host rebuilds from scratch again.
-e .
streamlit>=1.37,<2
pandas>=2.1,<4
PyYAML>=6,<7
pydantic>=2,<3
tzdata>=2024.1
