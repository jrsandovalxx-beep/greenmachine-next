# GMR-002 — completed record

- Approved implementation SHA: `3e01ad3e4b90326f478083e727f97f9b2fe71ca3`
- Verdict date: 2026-07-27
- Verdict-of-record sha256: `d934523ec0d834bd45b40a9c93a765d61e5c2d80d412e69445ec7a40738887ea` (certified by the reviewer, 2026-07-28)
- ticket text: REBUILD_PLAN §GMR-002 @ f9c9089f036011678ba1c5d71bace3d1ea2895524e68a9777df8042085397443

````VERDICT
**Files received:** `15_GMR002_REVIEW_PACKAGE_V2.md`, `16_GMR002_SOURCE_BLOB_BASE64.md`, and `06_REBUILD_PLAN_11.md`.

**Computed `sha256(06_REBUILD_PLAN_11.md)`:** `f9c9089f036011678ba1c5d71bace3d1ea2895524e68a9777df8042085397443` — exact match to the approved plan hash.

# APPROVED WITH NOTES

**Review object:** GMR-002 implementation PR, Regime A
**Approved head SHA:** `3e01ad3e4b90326f478083e727f97f9b2fe71ca3`
**Base:** `staging` at `ca7aa6cf3efd4382d07d3ef3af539872be05e685`

The evidence supplement cures both deficiencies from the prior **INSUFFICIENT EVIDENCE** verdict. GMR-002 implementation criteria 1–8 are met.

## Gap 1 — Source archive

**REASONING — Remediated.**

I independently decoded both fenced base64 entries in `16_GMR002_SOURCE_BLOB_BASE64.md`.

| Entry        | Decoded bytes | Computed SHA-256                                                   |
| ------------ | ------------: | ------------------------------------------------------------------ |
| `ci.yml`     |         1,014 | `1418c523ffa620f806bb885aa74caecfada4836b6841636b8bfb825db7fb7fc2` |
| `SHA256SUMS` |            73 | `278d8e3dc9bc83246397e069c7b2f315bfcffe815c249b657ea4d84ebc855a6e` |

Both byte counts and hashes exactly match the transfer document's index. The decoded `SHA256SUMS` contains:

```text
1418c523ffa620f806bb885aa74caecfada4836b6841636b8bfb825db7fb7fc2 *ci.yml
```

I also reconstructed `.github/workflows/ci.yml` from Appendix A of the review package. It is exactly 1,014 bytes and is byte-for-byte identical to the decoded source blob.

This independently establishes:

* source-blob hash = manifest hash;
* source-blob hash = committed-blob hash;
* source bytes = committed bytes;
* deviations = zero;
* the transfer contains only the stated `ci.yml` and `SHA256SUMS` entries.

The standing source-archive requirement is satisfied.

## Gap 2 — Protection regression comparison

**REASONING — Remediated, with a capture-method note.**

I parsed the full raw post-change protection JSON for both `main` and `staging`, applied the package's stated projection mechanically, and compared each result with the retained pre-change projection.

For both branches, the only projected difference is:

```text
required_status_checks: null
```

becoming a required-check object containing:

* `Format, lint, type check, test (Python 3.11)`;
* `Format, lint, type check, test (Python 3.12)`;
* `strict: false`.

All other projected controls remain equal:

* pull requests required;
* zero required GitHub approving reviews;
* stale-review dismissal off;
* code-owner review requirement off;
* admin enforcement on;
* force pushes disallowed;
* deletions disallowed;
* linear-history requirement off;
* conversation-resolution requirement off;
* branch locking off;
* fork syncing off;
* restrictions absent.

The full raw post-change responses also show, on both branches:

* `block_creations: false`;
* `required_signatures: false`;
* `require_last_push_approval: false`.

The package identifies these omitted pre-change projection fields and dispositions them rather than concealing them. No submitted evidence indicates that any of them was enabled before the status-check change, and none contradicts the GMN-000A-approved configuration. The existing live direct-push tests additionally show that PR enforcement and the two required checks are active.

### Capture-method note

**REASONING — Non-blocking.**

The retained pre-change records were complete for the stated projection, but were not raw API captures. Therefore this is not literal raw-JSON-to-raw-JSON equality across every response field.

I accept the evidence in this historical round because:

* the comparison uses the same projection on both sides;
* the projection covers the controls bound by the GMN-000A approval and the GMR-002 protection criterion;
* complete raw post-change JSON is provided;
* omitted fields are individually disclosed and currently false;
* the direct-push behavior was re-tested on both branches;
* no conflicting or unexplained policy change appears.

This approval does **not** establish projected pre-change captures as the preferred future standard. Future mutable-configuration changes should retain the full raw before and after responses whenever the plan requires a full configuration diff.

## Criteria 1–8

| Criterion                                                       | Determination                              |
| --------------------------------------------------------------- | ------------------------------------------ |
| 1 — Plan binding and active pointer                             | **Met**                                    |
| 2 — Provenance, source archive and zero deviations              | **Met**                                    |
| 3 — Green real-PR run                                           | **Met under Regime A evidence**            |
| 4 — Four gates demonstrated red then green                      | **Met**                                    |
| 5 — Red blocks merge; green restores mergeability; demo removed | **Met**                                    |
| 6 — Protection regression check and live branch re-tests        | **Met with the capture-method note above** |
| 7 — Wall clock below ten minutes                                | **Met**                                    |
| 8 — Checker clean and revision binding                          | **Met**                                    |

## Required matrix-context ruling

**REASONING — Approved.**

Both Python matrix contexts are correctly required on both protected branches.

Requiring only one would make the other advisory and would leave either the declared Python 3.11 floor or the Python 3.12 development environment outside the merge gate. `fail-fast: false` ensures both contexts report, and the submitted real-PR evidence demonstrates that both context names exist and complete successfully.

The fact that each deliberate violation fails on both Python legs is expected. Gate isolation is established by the failed step name—`Format check`, `Lint`, `Type check`, or `Test`—rather than by requiring only one matrix job to turn red.

## Unchanged-head binding

**REASONING — Adequately demonstrated under Regime A.**

The supplement states and re-evidences:

* branch `gmr-002-ci`;
* clean tracking state;
* `git rev-parse HEAD` = `3e01ad3e4b90326f478083e727f97f9b2fe71ca3`;
* PR #4 `headRefOid` = the same SHA;
* plan hash unchanged;
* checker still clean;
* no supplemental commit was made.

The approval remains bound to the original reviewed implementation head.

## Regime A limitations

**UNVERIFIABLE**

From attachments alone, I cannot independently establish that:

* PR #4 currently remains open at the approved head;
* the referenced GitHub Actions runs remain associated with that revision;
* the remote branch-protection configuration has not changed since the captures;
* the pasted API and Git outputs originated from the stated remote repository.

These are normal Regime A limitations and do not defeat the completed package.

**Head `3e01ad3e4b90326f478083e727f97f9b2fe71ca3` is approved to merge into `staging` under D-018.**

The GMR-002 closeout PR is a separate **Regime B** review object and is not approved by this verdict.
````
