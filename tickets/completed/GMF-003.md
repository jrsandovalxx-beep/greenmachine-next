# GMF-003 — completed record

- Approved implementation SHA: `161161457957ab7b178ba8ab802aa4e8ea5525a4`, merged to
  `staging` as `dae0b42031d5d3c1d487fdb5972930f8ffd41c59`
- Verdict date: 2026-08-16 — the date the verdict was received and relayed. The
  verdict text carries no date of its own; this is not presented as an issued-date.
- Verdict-of-record sha256: `35e6b27af2d0be4e285e5798c9af5d759f0a0d0098b7a2bdd3cf8b6403accc07`
  (certified by the reviewer)
- ticket text: FEATURE_PHASE_PLAN §GMF-003 @ sha256 83780d850e60c28a6a74fc03951428622ba8b20c9d7612998d378cd21d9c96cd
- historical plan untouched: sha256(tickets/REBUILD_PLAN.md) = c41b6abd5fbe64c0e9b468bea0c9a2ddb5f1b646780c017655f0295aa0b82ca2

````VERDICT
Receipt verified under D-047 **before reading the package**. All six envelope-part byte counts and SHA-256 digests match the supplied index; all four decoded artifacts also reproduce their declared digests. The decoded review package `GMF003_S1_PACKAGE_V2.md` is **201,159 bytes, 4,453 LF-terminated lines, SHA-256 `1dd549782e907545ef81976a01c828de6106b3caed30540e45bbe96c9d52277d`**. The V2 cover identifies the same reviewed head and the four intended blocker remediations. The package envelope itself declares the same decode target.

I also independently reproduced §13 at **156,961 bytes, 3,627 lines, SHA-256 `2c4738de1c03a0dbd988f3b2cb914105ad9ce7b7bfa4b54e41cae1319af0f6c7`**, using the separator-LF interpretation disclosed by the Lead; reconstructed all **8/8 new files** against their git blob SHA-1 prefixes; counted **20 diff files / 20 `index` lines**; and reproduced the package's **46-token** outside-diff SHA audit.

# APPROVED WITH NOTES

**Review object:** §GMF-003 submission 1 V2 — implementation, object 1 of 2  
**Approved head SHA:** `161161457957ab7b178ba8ab802aa4e8ea5525a4`  
**Base:** `staging` @ `c40a43141d5ec13cbe25815dbfe596d30968f015`  
**PR:** #24

## Criterion 1 — plan binding and active ticket

**REASONING — PASS.**

The plan hashes remain the pinned values, `ACTIVE.md` remains on GMF-003, no plan or decision-file revision is introduced, and the implementation delta remains bound to this object.

The additional `src/greenmachine/config/loader.py` hunk is addressed separately below. I do not treat its pathname alone as a scope violation because D-049 expressly makes permitted deltas **content-scoped rather than path-scoped**.

## Criterion 2 — current-season window, named rather than implied

**REASONING — PASS.**

No relevant V1 reasoning is disturbed. The contract explicitly binds pitch-type splits to a named window, the screen explicitly requests `SEASON_TO_DATE`, and its visible window name is read from the structure that produced the rows.

The ratified “fixtures cannot provide a semantic property the contract does not represent” problem remains closed.

## Criterion 3 — threshold, suppression and three usage states

**REASONING — PASS.**

The three-state treatment remains correct:

- numeric usage ≥15% qualifies;
- numeric usage <15% is measured suppression and is acknowledged rather than silently removed;
- absent usage remains unevaluable and retains the absence reason.

Eligibility continues to depend on usage alone. Metric-level absence does not suppress a qualifying pitch type.

The selected render independently confirms the intended visible distinction: `CU (0.050)` and `SI (0)` are identified as measured/below threshold, while `FS` and `KC` are separately unevaluable with distinct absence reasons.

## Criterion 4 — denominators and provenance

**REASONING — PASS. V1 BLOCKER CLOSED.**

The denominator portion remains compliant with the scoped criterion-4 ruling: Usage%, Barrel Rate, Exit Velocity, ISO, xwOBA, Whiff%, and SwStr% retain their distinct observation bases rather than being forced onto BBE.

The V1 provenance blocker is materially fixed. `provenance_notes()` now classifies provenance **per row within each metric**, with three distinguishable cases:

- all shown rows sourced;
- mixed sourced/derived, naming the derived row(s) with formula and the sourced complement by row;
- all shown rows derived, naming every row/formula.

That closes the exact V1 failure mode in which one derived value could sit beside sourced values while only a column-level “derived” statement appeared. The accompanying tests exercise the mixed case, keep the sourced complement explicit, prevent the derived clause from leaking onto sourced row names, isolate the distinction to one metric, and exercise the all-derived case. The Lead's cover accurately describes this remediation.

One semantic nuance is worth preserving in future work: an absent direct-source field can still have `derivation=None`, so “from source” on this surface should continue to mean **source pathway / not product-derived**, not “the source successfully returned a numeric value.” The separate absence text currently prevents that distinction from collapsing. This is non-blocking.

## Criterion 5 — reuse of the GMF-002 grid

**REASONING — PASS.**

The earlier approval stands. The generalized frame/rendering machinery remains one implementation rather than a second pitch-type copy, while the original GMF-002 path retains its defaults and prior formatting semantics.

I find no reopening of the forbidden GMF-002 null-cell representation.

## Criterion 6 — fixtures, AppTest and local render

**REASONING — PASS. V1 EVIDENCE BLOCKER CLOSED.**

The render evidence is now actually delivered rather than referenced.

I independently decoded and inspected all three PNGs. The initial full-page render is **110,421 bytes, SHA-256 `38136fff09fb3035e6e296968f89ef8e549d5f6bc4bb35ccb52736dc84dbe46a`**; its D-047 envelope binds it to the reviewed head. It visibly shows:

- Commit `1611614`;
- an empty `Batter` chooser;
- the `Choose a batter` placeholder;
- the invitation saying nothing is selected for the user;
- no pitch-type data surface before user action.

The selected full-page image and metrics crop also reproduce their declared hashes. They visibly show the seven-column surface, `SEASON_TO_DATE`, the stated ≥15% threshold, qualifying CH/FF/SL rows, measured suppression of CU/SI, separate unevaluable FS/KC states, and SL's unavailable xwOBA rendering as the component's `None` **with the full source-unavailable reason stated immediately below the table**.

That is exactly the additive remedy authorized under ruling 9: the inherited cell representation remains untouched while §GMF-003 provides readable text around it.

The package additionally reports a pinned Streamlit 1.37.1 AppTest run of **30 passed, zero skipped** for `tests/app`; I can inspect the committed tests in the diff but cannot independently execute that environment from the review artifacts. The supplied test evidence is consistent with the implementation.

## D-015/D-017 — automated hitter selection

**REASONING — PASS. V1 BLOCKER CLOSED.**

V1's `index=0` defect is gone.

The implementation now uses `index=None`, returns before constructing a split screen while the chooser is empty, and renders an explicit no-selection invitation. The selected surface appears only after a user-composed chooser value exists.

The composited initial render independently confirms that behavioral state.

The separate batter chooser therefore no longer constitutes automated hitter selection.

## D-047 — transport

**REASONING — PASS. V1 BLOCKER CLOSED.**

This delivery follows the standing envelope rule rather than asking for an exception. Every package/render artifact reached the review as base64 inside Markdown envelope parts with strip-whitespace/reassembly instructions.

I verified the **raw six part files first**, then decoded them, then verified the four artifact digests. No byte mismatch occurred.

## `src/greenmachine/config/loader.py` — scope ruling

**REASONING — ACCEPTED. Non-blocking incidental gate repair; no plan revision required.**

The Lead correctly surfaced this instead of assuming it was allowed.

The complete hunk does only two substantive things:

1. removes the now-obsolete `# type: ignore[no-untyped-call]` from `check_event`;
2. updates the adjacent explanation while leaving the still-required `peek_event` ignore intact.

There is no runtime algorithm change.

On the supplied record, the change was triggered by toolchain drift after V1 and was needed to restore the mandatory strict-type-check gate. The final object criterion itself requires the gate to be green. D-049 does not prohibit a file merely because another ticket originally introduced it; it prohibits using incidental work to widen the **content** authorized by the plan.

I therefore rule this exact maintenance hunk **in scope as incidental gate repair**. This does **not** create a general rule that “CI is red” authorizes unrelated maintenance. The authorization is narrow: an externally induced checker failure, a minimal non-product-semantic correction to the obsolete suppression causing that failure, disclosed in the package.

## Gates, sweeps and Regime B

**REASONING — PASS on supplied/reproducible evidence; mutable remote state UNVERIFIABLE — Non-blocking.**

The package reports:

- format clean;
- lint clean;
- strict mypy clean;
- `2917 passed, 5 skipped`;
- consistency checker 10/10;
- successful push and pull-request CI at exact head `161161457957ab7b178ba8ab802aa4e8ea5525a4`;
- CLEAN merge gate.

The package deliberately retains the failed intermediate `5a7c8ab` CI runs, which is the correct record rather than silently dropping them. The Lead identifies those runs and the succeeding reviewed-head pair in its cover.

My independent scan of the supplied diff finds **zero** of the twelve D-015/D-017 forbidden textual patterns in added lines, no added network-call implementation, no added absolute/home-directory path, and no apparent secret value.

Remote CI/gate state is mutable and cannot be independently reproduced from these static artifacts, so the standing **exact-head CI and merge-gate recheck immediately before merge remains required**.

## Evidence note — §14 diff-token wording

**REASONING — NOTE; non-blocking.**

The package's numerical statement that the diff fence contains **41 SHA-shaped tokens** is correct. The explanatory sentence immediately following it is slightly overbroad.

My enumeration finds:

- **40** git blob tokens from the 20 `index` lines; plus
- **1** additional hex-shaped token, `20260815`, from the types-PyYAML release date in the loader comment.

So all 41 are **not** git-generated blob identities. This does not undermine the diff digest, the 8/8 reconstructed blobs, or the outside-diff 46-occurrence reconciliation; it is a classification/prose defect in the package audit.

If this language is reused in the closeout evidence, correct it to classify the 40 blob tokens and the one date token separately.

## State

**Head `161161457957ab7b178ba8ab802aa4e8ea5525a4` is APPROVED to merge into `staging`, subject to the standing immediately-pre-merge exact-head CI and merge-gate recheck.**

After that exact SHA merges, **GMF-003 object 2 — the fixed four-file closeout — becomes authorized.** No additional implementation submission or deployed-verification object is required by this approval.
````
