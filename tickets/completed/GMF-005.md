# GMF-005 — Park, Weather, and Venue Context Inputs

**Status**: completed 2026-08-20 via PR #28 (merge commit
`2990f408894e606449bb5fbce9349042266bff11`, base `staging`).

## Delivered

- Canonical venue reference `greenmachine.inputs.park_reference` — all 30
  venues with coordinates, roof classes, altitude bands, and Savant park-factor
  file slugs; registry rejects duplicates, blanks, and non-30 cardinality.
- Savant per-handedness park-factor snapshot
  `data/savant_park_factors_2024-2026.csv` (59 rows, per-PA weights,
  mixed handedness, 2024–2026 pooled) bound through
  `greenmachine.inputs.savant_park_factors`; Savant's blank-text factor cell
  for Oakland read as 100 under the documented mean-neutral convention.
- `ParkVenue` / `ParkFactor` / `WeatherForecast` / `ParkInputs` contract
  values in `greenmachine.inputs.contract` with literal-source provenance and
  typed absence (`NOT_APPLICABLE` for roofed venues, `NOT_YET_OBSERVED`,
  `SOURCE_UNAVAILABLE`).
- `ParksScreen` (/parks) plus shell and surface registrations; roster and
  roof-structure matrix render from the reference/snapshot binding.
- Live weather: `NwsWeatherAdapter` (`greenmachine.weather.nws`) over the
  host-pinned `UrllibTransport` (api.weather.gov only, redirect
  re-validation, bounded timeout); the app binds the weather field live when
  deployed and falls back to the pinned fixture otherwise. Network transport
  is composition-root only; `src/` tests use fake transports.

## Verification at merge

- `python3 scripts/check_consistency.py` — 10/10 on the merged tree.
- `pytest` — 3099 passed, 1 skipped (known CI-environment skip) on Python
  3.11; ruff format/check and mypy --strict clean.
- Live adapter exercised against api.weather.gov from the deployed app
  binding path.

## Notes

- Merged under the PO's 2026-08-20 autonomous-build directive granting
  merge/push authority; local verification (checker + full suite on the exact
  merged tree) stood in for the pre-2026-08-20 review-chat gate (see D-069).
