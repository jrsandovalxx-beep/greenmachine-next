# GMF-004 — completed record

- Approved implementation SHA: `0e45d3ed29d829ba6dc35f6b802af45d975c1576`, merged to
  `staging` as `5594e0f118dfc77a6aada18ef487a1bc723bb000` — the merge commit and the
  post-merge `staging` head are the same commit, re-verified against the tree.
- Verdict date: 2026-08-18 — the date the verdict was received and relayed. The
  verdict text carries no date of its own; this is not presented as an issued-date.
- Verdict-of-record sha256: `53bbe0ea5383307a35601e60300612472757c1970fb207fa255a41816fa07f74`
  (certified by the reviewer)
- ticket text: FEATURE_PHASE_PLAN §GMF-004 @ sha256 83780d850e60c28a6a74fc03951428622ba8b20c9d7612998d378cd21d9c96cd
- historical plan untouched: sha256(tickets/REBUILD_PLAN.md) = c41b6abd5fbe64c0e9b468bea0c9a2ddb5f1b646780c017655f0295aa0b82ca2

````VERDICT
Receipt verification completed under D-047 before substantive review. The 788-byte envelope index and all four raw envelope identities reproduce exactly; after whitespace-stripping/base64 decode, the package and all three PNGs also reproduce their declared byte counts and SHA-256 values. The envelope index binds every part to reviewed head `0e45d3ed29d829ba6dc35f6b802af45d975c1576`. The individual render envelopes likewise bind the three visual artifacts to that exact head.

# APPROVED WITH NOTES

**Review object:** §GMF-004 submission 1 — implementation, object 1 of 2  
**Approved head:** `0e45d3ed29d829ba6dc35f6b802af45d975c1576`  
**Base:** `staging` @ `92d4652bfba32c644b923e9c2601b2c31bfe7e5c`  
**PR:** #26  
**Branch:** `gmf-004-parks`

The governing plan fixes GMF-004 at two objects and requires the pinned handedness park factors, one fixture-bound weather seam, distinct D-055 roof handling, no excluded provider/client/credential, and AppTest plus local render.

## Criterion 1 — plan and revision binding

**REASONING — PASS.**

The package carries the pinned plan hashes:

- FEATURE_PHASE_PLAN: `83780d850e60c28a6a74fc03951428622ba8b20c9d7612998d378cd21d9c96cd`
- REBUILD_PLAN: `c41b6abd5fbe64c0e9b468bea0c9a2ddb5f1b646780c017655f0295aa0b82ca2`

and `ACTIVE.md` names GMF-004.

The revision evidence consistently binds:

- base `92d4652bfba32c644b923e9c2601b2c31bfe7e5c`;
- exactly two commits over it;
- intermediate implementation `4d416bb04790d678d9a8c1afdc7a17fe8de97484`;
- final reviewed head `0e45d3ed29d829ba6dc35f6b802af45d975c1576`.

I independently extracted the embedded diff and reproduce **1,968 LF / 81,174 bytes / SHA-256 `faa96c7a1a21dad3b2fcc0f8ed60800fb472836abd7b562af0169c7db8bad47c`**.

It contains exactly **15 files: 5 modified, 10 added**.

All **10/10 new files** independently reconstructed from the diff's `+` lines reproduce their own git blob SHA-1 `index` identities.

The second commit's one-line caption correction is legitimate evidence-driven work rather than unreviewed feature expansion: the prior global statement that the page contained synthetic fixtures only became false once criterion 2's real pinned Savant export appeared.

## Criterion 2 — pinned Savant factors by handedness beside venue type

**REASONING — PASS.**

The new reader authenticates the already-approved committed CSV **before parsing**, against the pinned:

`2bbaee9d049008bdd9887f8c68feecc683c4e1803513b12c9797cb9037252ebf`

It then enforces 58 rows, 29 represented venues, two handedness rows per represented venue, and the uniform `2024-2026` rolling window. It deliberately consumes `index_hr`, rather than silently taking the adjacent headline `index_woba`.

That is consistent with the original GMF-001 provenance record, which establishes two manually saved handedness pages, the combined 58-row derived CSV, the same pinned digest, and `n_pa` as the sample behind each row.

The rendered parks table visibly places:

`Venue type → HR factor (LHB) → HR factor (RHB)`

and every present factor carries its PA sample beside it, e.g. `94 · PA 22582`.

The Athletics/Sutter Health Park gap remains an absence rather than zero or a league-average substitution.

**Provenance note — non-blocking.** `ManualExportProvenance` has only one `source_url` field by the already-approved contract. GMF-004 populates that runtime field with the LHB form of the Savant URL even though the pinned combined CSV was derived from separate LHB and RHB pages. The authoritative committed GMF-001 provenance record still preserves **both** acquisition URLs and the combined-file derivation, so I do not treat this as loss of the certified acquisition chain. Future prose should not describe the runtime single-URL field as the *entire* two-page acquisition record.

## Criterion 3 — one weather seam, fixture-bound, unavailable supported

**REASONING — PASS.**

There is one `WeatherAdapter` protocol with one relevant operation returning `SnapshotField[WeatherForecast]`.

That design makes unavailable weather an ordinary contract value rather than an exception path.

The GMF-004 binding is synthetic:

- roof state and weather are fixture data;
- the test values are deliberately non-baseball;
- coverage is explicitly enumerated;
- an unnamed venue defaults to `NOT_YET_OBSERVED`, not an invented forecast;
- Rogers Centre exercises `SOURCE_UNAVAILABLE`;
- Fenway exercises a separate `NOT_YET_OBSERVED` forecast absence.

The rendered page clearly tells the reader that park factors are the real pinned export while conditions remain fixture-bound until §GMF-005.

## Criterion 4 — D-055 roof states and forecast suppression

**REASONING — PASS.**

D-055 requires venue type to matter, suppresses forecasts for fixed/closed roofs, applies weather to open air, treats retractable UNKNOWN explicitly, and calls out the unavailable Rogers Centre case.

The implementation provides a total rendering decision rather than scattered display conditions:

- open air → forecast applies;
- retractable OPEN → applies;
- retractable CLOSED → suppressed;
- fixed roof → suppressed;
- retractable UNKNOWN → withheld;
- retractable roof state not obtained → withheld.

The last case is the ratified Q3 extension and is logically conservative: if the roof's actual state was not obtained, printing forecast values could print them for a park whose roof is in fact closed, violating the criterion's hard prohibition.

The supplied render independently confirms the distinctions. In particular it visibly separates:

- **closed-roof suppression:** Daikin Park, T-Mobile Park, Tropicana Field;
- **roof-state-undetermined withholding:** Globe Life Field and loanDepot park;
- **forecast applies but adapter lacks a value:** Fenway Park and Rogers Centre.

No forecast value is printed in the closed or undetermined groups.

## Criterion 5 — no Ballpark Pal, provider call, credential

**REASONING — PASS.**

My own added-code scan finds no HTTP/client/socket implementation in this object. The Savant reader's only operative input is the committed local file; the MLB URL is provenance text, not a dialed endpoint.

I also reproduce zero added secret/home-directory shapes and no Ballpark Pal or Seamheads implementation.

This remains consistent with the governing provider rules: Ballpark Pal is excluded and the project has no credential at this stage.

The reader module contains a prose sentence saying “There is no URL here” even though a provenance URL literal appears later in that same module. Read literally, that sentence is false; read in context, its intended claim is clearly “there is no fetch URL/path here.” The implementation and tests establish the latter. **Prose note only; non-blocking.**

## Criterion 6 — fixtures, AppTest and local render

**REASONING — PASS.**

The apparent “fixtures only” / pinned-data tension cannot rationally mean that the required park factors themselves must become synthetic: criterion 2 expressly requires the **pinned Savant factors**. The coherent scope is the one the implementation uses—pinned factors where criterion 2 requires them, synthetic fixture-bound roof/weather conditions and synthetic test data elsewhere. Reading criterion 6 as prohibiting the criterion-2 data would produce the D-050 impossible-criterion shape.

The package reports the pinned-floor AppTest run as **40 passed, zero skipped**.

More importantly for evidence that AppTest cannot establish visually, all three actual PNG artifacts were delivered and independently inspected:

- corrected global caption and Commit `0e45d3e`;
- neutral alphabetical initial park order;
- venue type beside both factor columns;
- PA beside every present factor;
- explicit separation of pinned factor provenance from synthetic conditions;
- closed-roof suppression;
- both forms of roof-undetermined withholding;
- two distinct adapter-absence reasons;
- Sutter Health Park's factor gap stated in words.

The inherited canvas `None` behavior remains untouched. The factor absence explanation is additive text around that inherited behavior, consistent with the already-fixed GMF-002 boundary.

## GMF-002 grid reuse

**REASONING — PASS.**

There is exactly one GMF-002-era implementation edit:

`src/greenmachine/grid/view.py` — **9 insertions, 0 deletions**.

It adds `ParkFactor` handling to the previously generalized dispatch:

- textual representation `factor · PA sample`;
- numeric extraction of the factor.

No existing dispatch branch is altered, and no old metric/null-cell behavior changes.

This is a proper continuation of the approved reusable mechanism, not a second grid implementation and not a reopening of the GMF-002 null-cell ruling.

## D-015/D-017

**REASONING — PASS.**

I independently scanned **all 1,766 added lines** against the fixed twelve forbidden patterns and obtain **zero hits**.

The parks table starts in alphabetical venue order. Sorting is user-controlled; there is no automatic “best park” ordering, selection, score or tier.

The green factor styling is inherited metric visualization, not an automatically selected recommendation.

## Adoption/provenance of the draft

**REASONING — ACCEPTED.**

I do not treat the parallel builder's uncommitted draft as a prior reviewed revision.

The relevant governance facts are:

- it never became the head of record;
- the lineage builder disclosed its origin;
- quiescence was measured before adoption;
- the received files were inventoried;
- defects were found rather than silently inherited;
- the defective fixture coverage behavior was rewritten;
- the actual review object remains the complete base→`0e45d3e…` diff.

The adopted draft therefore has no independent approval standing; only the exact SHA named by this verdict does.

## Audit exactness note

**REASONING — NOTE; non-blocking, but the package's 84-occurrence assertion is not accepted as exact.**

The diff-local §14 accounting is good: I independently reproduce **31** qualifying tokens inside the diff itself—30 from the 15 `index` lines plus the pinned CSV SHA—and the ten null pre-images are correctly distinguished from blob identities.

The broader package audit, however, says its scanner covers **“every hex run of seven or more characters containing at least one digit”** outside the stated exclusions and then claims exactly 84 enumerated occurrences.

Under that literal rule I can identify qualifying occurrences absent from its 84-row table, including the seven-digit fractional-second portions of the four Regime-B timestamps:

`4704520`  
`5682108`  
`1279616`  
`7692898`

There are also places where the reconstruction evidence carries both a full new-file blob SHA-1 and its separate seven-character `index` abbreviation on the same line; a literal occurrence scanner must account for both, not one row for the line.

So the claimed **build-fatal exact occurrence accounting is stronger than what the delivered table demonstrates**.

This does **not** undermine the implementation diff, the 10/10 reconstructed blobs, the D-015/D-017 scan, the reviewed-head binding, or my independent rejected-head checks. I therefore treat it as a package-audit mechanism defect rather than an implementation blocker.

**Do not carry the “84 exact occurrences / every hex run” assertion into the GMF-004 closeout as established fact.** If the audit mechanism is reused, either make its lexical exclusions explicit—e.g. timestamp fractions—or actually enumerate every occurrence its stated grammar matches.

## Gates and Regime B

**REASONING — supplied evidence consistent; mutable remote state UNVERIFIABLE — non-blocking.**

The package reports at exact head `0e45d3ed29d829ba6dc35f6b802af45d975c1576`:

- local `3014 passed, 5 skipped`;
- floor `tests/app`: `40 passed, 0 skipped`;
- consistency checker 10/10;
- pull_request run `32097249730`;
- push run `32097247538`;
- both Python legs: `3018 passed, 1 skipped`;
- PR OPEN / MERGEABLE / CLEAN.

It also retains the intermediate `4d416bb…` CI history rather than rewriting the record after the caption correction.

Remote CI/gate state remains mutable, so—as in the previous objects—the standing **immediately-before-merge exact-head CI and merge-gate recheck remains mandatory**.

## State

**Head `0e45d3ed29d829ba6dc35f6b802af45d975c1576` is APPROVED to merge into `staging`, subject to the standing immediately-pre-merge exact-head CI and merge-gate recheck.**

After that exact SHA merges:

- **GMF-004 object 1 of 2 is complete;**
- the fixed four-file **GMF-004 closeout becomes authorized** as object 2 of 2;
- the decision register remains at **62** unless separately authorized by a plan/decision object.

No other implementation head is authorized by this verdict.
````
