# GMR-004 — completed record

- Approved submission 1 SHA: `72a31e44bcd98b94c1256e82547ae6ab29395bda`
- Approved submission 2 head of record: `500613ec81dd972a3ad53d8f4ec618665ad90c5f`
- Verdict date: 2026-08-03 — the date the verdicts were received and relayed. The
  verdict texts carry no date of their own; this is not presented as an issued-date.
- Verdict-of-record sha256 (certified by the reviewer):
  submission 1 `1e46e7bd2deb10c6a9abb43f758278d452487f2f1f441cc33ab06bc92e939701`,
  submission 2 `5b68f17985958d1c4d612802f835c6bad1d80a3cb1307625499b7835de128f02`
- ticket text: REBUILD_PLAN §GMR-004 @ sha256 3daa28b952f3c78d80c2d01764f495e3866eb68e32d13de86ac6f7a67646d904

## Submission 1 verdict

````VERDICT
**Files received:** `38_GMR004_S1_REVIEW_PACKAGE.md`.

**Computed `sha256(06_REBUILD_PLAN_22.md)`:** `3daa28b952f3c78d80c2d01764f495e3866eb68e32d13de86ac6f7a67646d904` — exact match to approved Plan v15.

# APPROVED WITH NOTES

**Review object:** §GMR-004 submission 1, Regime B
**Approved head SHA:** `72a31e44bcd98b94c1256e82547ae6ab29395bda`
**Base:** `staging` at `f6e7aa3c7f802c52572d3cc20953625cee80e0f8`
**PR:** #11

Head `72a31e44bcd98b94c1256e82547ae6ab29395bda` satisfies submission 1 criteria 1, 2, 3(a–f), 4, 4b and 5. This approval does not cover submission 2 or GMR-004 closeout.

## Independent diff verification

**REASONING — Verified.**

I extracted the inline four-backtick diff with its final newline and independently reproduced:

* 11,724 bytes;
* 235 lines;
* SHA-256
`979583ce2bc0b4c511dccac3ce2a19f89fd884dbc586996c0d27e35758b6fb35`.

The diff contains exactly three paths:

* added: `docs/DEPLOYMENT.md`;
* modified: `pyproject.toml`;
* added: `streamlit_app.py`.

There is no fourth or unrelated path. Both added Python/Markdown files reconstruct cleanly, and `streamlit_app.py` parses as valid Python.

## Criterion 1 — plan and revision binding

**REASONING — Met.**

The package quotes the correct Plan v15 section, records the approved plan hash, shows `tickets/ACTIVE.md` on GMR-004, binds the branch to the verbatim head SHA and supplies the complete base-to-head diff.
The stated merge base equals the submitted base, making the delivered three-dot diff the complete PR delta.

## Criteria 2 and 3 — shell and resolution routes

**REASONING — Met.**

The committed shell renders:

* name;
* environment;
* installed package version;
* commit.

The local AppTest result binds the rendered commit to short SHA `72a31e4`.
All mandatory routes are exercised:

* unset environment → `local`;
* `staging` → `staging`;
* `production` → `production`;
* package absent → version `unknown`;
* no Git and no `GM_COMMIT` → `unknown`;
* no Git with `GM_COMMIT=deadbee` → `deadbee (env)`;
* detached HEAD → `72a31e4`;
* Git plus a contradictory `GM_COMMIT` → Git SHA wins.

The implementation’s Git-first order matches the approved plan. Expected failures from Git absence, detached symbolic-ref lookup and missing package metadata are converted to explicit display fallbacks rather than page crashes.

## Criterion 4 — requirements-contract evidence

**REASONING — Met.**

The operative criterion explicitly says the scope prose is not itself the criterion. It requires the final file, dependency bounds, the changed contract’s node identity, old-contract failure, updated-contract success and the committed D-030 supersession. All are supplied.
I accept the historically completed contract update rather than requiring a manufactured change in this PR because:

* the updated contract is byte-identical at the tag, base and reviewed head;
* GMR-003 was required to transplant that test without deviation;
* the old four-line contract fails against the final file specifically because of `-e .`;
* the updated contract passes against the final file;
* the updated contract also fails against an old-shaped temporary file, proving that the two contracts materially disagree;
* altering the already-correct test merely to create an in-ticket diff would be an unenumerated change.

This is evidence that the contract changed, not a narrative substitute.
The final dependency lines all contain version specifiers. `tzdata>=2024.1` is bounded below even though it has no ceiling; it is not an unversioned dependency. Its floor-only form is also the exact contract-pinned and `pyproject.toml`-matched value.

## Criterion 4b — two deselections re-enabled

**REASONING — Met.**

I independently reproduced the Plan v15 table hashes:

* §GMR-003 pre-edit table: 1,363 bytes, SHA-256
`39cf4f67ecc9370487e13c32a13cd45c214600847a4a75496eca9a137b0d1b2d`;
* §GMR-004 resulting table: 1,080 bytes, SHA-256
`9679ad244fa03c18c662c42948ccfe5999f30ae8d1dc1aa1e030bc947b7fdac6`.

Removing exactly the two `[streamlit_app.py]` deselections from the first table yields the second table byte-for-byte. The diff performs exactly that transformation and leaves four deselections.
The two required node IDs are shown passing locally at the reviewed head. The CI proof is indirect but logically sufficient:

* base and head collect the same 2,619 nodes;
* the selected-set diff adds exactly the two named `[streamlit_app.py]` nodes;
* CI at the reviewed head reports exactly four deselections;
* CI reports 2,615 passed with zero failed or skipped nodes on both Python legs.

Therefore the two newly selected nodes passed in each CI leg.
**Approval note:** The node IDs are not printed individually by the fixed bare-`pytest` workflow. This approval accepts the exact selected-set delta plus the all-passed CI summary as proof “by node ID in the CI run”; it does not treat an unqualified aggregate count alone as sufficient evidence.

## Secrets bridge

**REASONING — Non-blocking note.**

The bridge does not alter the plan’s required Git-first commit resolution and its local no-Git demonstration succeeds.
However, Streamlit’s official documentation says root-level `st.secrets` values are already exposed as environment variables. On Community Cloud, the added bridge is therefore likely redundant for these two root-level keys, and the broader assertion that a separately supplied “real environment variable” always wins over a same-named Streamlit secret is not independently established by this package. ([docs.streamlit.io](https://docs.streamlit.io/develop/concepts/connections/secrets-management?utm_source=chatgpt.com))
This is not a submission 1 blocker because:

* Git still wins over `GM_COMMIT`;
* the required fallback remains an environment-variable lookup;
* root-level Community Cloud secrets provide that environment value;
* submission 2 must demonstrate the actual deployed environment and commit behavior.

Submission 2 must report the host’s observed route rather than relying on the bridge description.

## Deployment documentation

**REASONING — Met for submission 1.**

`docs/DEPLOYMENT.md` identifies the staging branch, entrypoint and requirements file and defines:

* initial staging setup;
* Git-first commit verification;
* the explicit `(env)` fallback route;
* redeployment steps;
* a protected-branch `git revert` rollback;
* tree-hash equality;
* display of the forward-moving revert SHA;
* disappearance of the visible rollback marker.

The absence of `.streamlit/config.toml` is not a submission 1 scope defect. The still-deselected deployment-contract node and its additional legacy evidence dependency remain assigned to GMR-005 under the approved deselection register.
Whether the documented Community Cloud procedure works verbatim is an execution question reserved for submission 2; approval here does not pre-approve the deployment or rollback evidence.

## Criterion 5 — prohibited surface, gates and checker

**REASONING — Met.**

The fixed D-015/D-017 pattern scan is clean over all three changed paths.
The package records:

* formatting clean;
* lint clean;
* strict mypy clean;
* local suite green with the four known platform skips;
* both required Ubuntu CI legs green;
* 2,615 passed and four deselected in each CI leg;
* repository consistency checker at eight checks and zero failures;
* pull-request head and CI `headSha` equal to the reviewed SHA.

The change of builder does not affect the result. The submitted revision is independently bound to its diff, tests and CI rather than to conversational continuity.

## Mutable remote-state boundary

**UNVERIFIABLE — Non-blocking.**

From the package alone I cannot guarantee that, at the instant of merge:

* PR #11 still points to `72a31e44bcd98b94c1256e82547ae6ab29395bda`;
* both required checks remain green;
* the merge gate remains `CLEAN`;
* no later commit has been pushed.

The normal pre-merge SHA and gate recheck remains required. Approval applies only to the named head.

**Head `72a31e44bcd98b94c1256e82547ae6ab29395bda` is approved to merge into `staging`. After merge, §GMR-004 submission 2 may proceed as the separate post-merge, pre-production review object.**
````

## Submission 2 verdict

````VERDICT
**Files received:** `39_GMR004_S2_REVIEW_PACKAGE.md`.
**Previously received and reused:** `06_REBUILD_PLAN_22.md` and `38_GMR004_S1_REVIEW_PACKAGE.md`.

**Computed `sha256(06_REBUILD_PLAN_22.md)`:** `3daa28b952f3c78d80c2d01764f495e3866eb68e32d13de86ac6f7a67646d904` — exact match to approved Plan v15.
**Computed `sha256(39_GMR004_S2_REVIEW_PACKAGE.md)`:** `7d0f2eea4dcbd41638f43aaeadf0b8e67daf3b8b02548038e22ab315d0ae2cf3`.

# APPROVED WITH NOTES

**Review object:** §GMR-004 submission 2, Regime B
**Approved head of record:** `500613ec81dd972a3ad53d8f4ec618665ad90c5f`
**Submission 1 merged baseline:** `7329f02759b8ee4f8608fd5409ccfd23125ec933`
**Reviewed intermediate staging states:** `54430c21854b1ec64d69b84bc66ab75916be6fec` and `12cdfdc5577e42799b7418137d0b200016e4d4b6`

The evidence satisfies §GMR-004 submission 2 criteria 6–9 and the observed-route report required by the submission 1 verdict. This approval covers the complete staged sequence represented by head of record `500613ec81dd972a3ad53d8f4ec618665ad90c5f`. It does not approve the GMR-004 closeout object.

## Revision and diff binding

**REASONING — Verified.**

I independently extracted all three embedded four-backtick diffs, preserving their final newlines:

| Diff                      | Bytes | Lines | SHA-256                                                            |
| ------------------------- | ----: | ----: | ------------------------------------------------------------------ |
| Baseline → head of record | 3,457 |    59 | `6aa07c50e3ffec297f8f0b7f7d220663f9e4f67e6b9ab77cc7b34dec258b3cc9` |
| PR #12 marker             | 1,545 |    31 | `efdde54ed4250b8e9c987aeba7075ac8bef57b387a6708e526c7a3c1fdc97e68` |
| PR #13 rollback           | 1,545 |    31 | `6b152b73cc5dad1e4415afde54cebc574c772ea100cdda725e994c2f10131474` |

The marker and rollback patches are exact inverses:

* PR #12 adds 14 lines and removes none;
* PR #13 removes those same 14 lines, in the same positions, and adds none;
* PR #12 changes `streamlit_app.py` from blob `d3ae7f9…` to `59e3059…`;
* PR #13 changes it from `59e3059…` back to `d3ae7f9…`.

I reconstructed submission 1’s approved `streamlit_app.py`; its full Git blob ID is:
`d3ae7f92dcdd8fea60eebac60020209f1073e7f8`
Applying PR #12 produces:
`59e3059c1f271fc5df2f99372e138dd86bada6aa`
Applying PR #13 restores the original bytes exactly.
The net submission diff therefore contains no `streamlit_app.py` delta and touches only `docs/DEPLOYMENT.md`. Applying that diff to submission 1’s approved document produces the stated final abbreviated blob `50225c2…`.

## Criterion 6 — deployed staging identity

**REASONING — Met under the timestamped deployment evidence.**

Before the marker sequence, the deployed page is recorded as rendering:

* environment: `staging`;
* version: `0.3.0`;
* commit: `7329f02`.

The commit equals the submitted short form of the staging baseline and carries no `(env)` provenance marker. The evidence therefore shows that the live Git-checkout route was active and that the fallback route was not silently displaying another value.

## Observed-route report

**REASONING — Met.**

The deployed marker reported:
`GM_ENVIRONMENT present, GM_COMMIT absent; bridge wrote: nothing`
Together with the shell’s previously approved resolution logic, that establishes for this deployment:

* the configured root-level `GM_ENVIRONMENT` value was already present in `os.environ`;
* the bridge did not supply or overwrite it;
* no `GM_COMMIT` fallback value was configured;
* the displayed commit therefore resolved through Git.

This observation agrees with Streamlit’s documented behavior that root-level secrets are exposed both through `st.secrets` and as environment variables. ([docs.streamlit.io](https://docs.streamlit.io/develop/concepts/connections/secrets-management?utm_source=chatgpt.com))
The corrected runbook appropriately retracts the earlier claim that Community Cloud does not expose the values as environment variables. It also correctly limits the remaining precedence statement to the bridge’s own behavior: the bridge does not overwrite an existing `os.environ` key. The untested host-level collision between a separately existing environment variable and a same-named exported secret remains unclaimed.

## Criterion 7 — redeploy test

**REASONING — Met.**

PR #12 introduced an unmistakable visible marker and its pre-bridge diagnostic. The package binds that PR to head `696cfd4bf0d328abe69a533273fe3c31d4e61033`, records both required CI events green and identifies its merge commit as:
`54430c21854b1ec64d69b84bc66ab75916be6fec`
The subsequent deployed-page capture shows both:

* commit `54430c2`;
* the new visible marker.

This is the exact pair criterion 7 requires: the merged visible change and the new staging SHA on the deployed page.

## Criterion 8 — objective rollback

**REASONING — Met.**

The rollback was executed through a separate `git revert -m 1` PR into protected `staging`, not through history rewriting.
All three required proofs are present:

1. **Content identity:** the post-revert staging tree and the pre-marker baseline tree are both reported as
   `6956970f16e238d7130e6eea203b3fbccc718888`.
   The exact inverse patches and reversed blob indices independently corroborate the `streamlit_app.py` portion of that whole-tree assertion.
2. **History moved forward:** the deployed app displayed `12cdfdc`, the rollback PR’s new merge commit—not the old baseline SHA.
3. **Marker removed:** the post-rollback capture omits the marker that was present in the criterion 7 capture.

The package’s history shows no intervening staging content between the marker merge and rollback merge. PR #14 was merged only after the rollback tree proof was captured, so its documentation change does not contaminate criterion 8(a).

## Criterion 9 — deployed-page leakage check

**REASONING — Met under submitted capture evidence.**

The submitted rendered text at the rollback and final states contains only:

* application name;
* deployment-shell caption;
* environment;
* version;
* short commit SHA.

The package also records a 15,638-byte app-document DOM scan with zero hits for the enumerated credential, traceback, local-path and secret indicators. No D-015/D-017 product surface appears in the rendered text.
The plan does not require a particular screenshot or DOM archival format, so the timestamped rendered-text transcript and disclosed DOM scan satisfy the criterion.

## PR #14 and the D-018 exception

**REASONING — Permitted.**

The governing standing rule does not limit the exception to only the marker and rollback PRs. It states that the sole pre-approval staging-merge exception is **GMR-004 submission 2**.
PR #14 is part of the submission 2 review object and contains only deployment-runbook corrections derived from the host observations generated by that object. Its changes:

* correct the secrets-export rationale;
* record the observed Git route;
* correct the host’s “Create app” label;
* document the Product Owner’s staging-visibility choice.

The corrections were also responsive to the submission 1 approval note requiring the actual host route to be reported rather than inferred. On the pinned plan’s wording, merging PR #14 before this verdict did not breach D-018.

**Approval note:** The package’s narrower paraphrase—“only for deployment-verification evidence”—is not the controlling text. The Plan v15 exception naming the submission 2 object is controlling.

## Public-visibility statement

**UNVERIFIABLE — Non-blocking.**

The runbook states that the app is public. Official Streamlit documentation says that a public app should be freely viewable at its app URL and that a private-repository deployment must be explicitly made public for unrestricted viewing. ([docs.streamlit.io](https://docs.streamlit.io/deploy/streamlit-community-cloud/share-your-app?utm_source=chatgpt.com))
My current automated attempt to open the supplied app URL was redirected to Streamlit’s authentication flow rather than yielding the app document. ([]())
That result does not conclusively prove the app is private—the automated retrieval environment may not reproduce an ordinary browser session—but it means I cannot independently certify the current public-visibility setting. This does not defeat criteria 6–9, which are supported by the package’s timestamped deployment sequence and do not require public visibility. The Product Owner should separately confirm the current “Who can view this app” setting before treating the runbook’s public-status sentence as continuing fact.

## Untested reboot fallback

**REASONING — Non-blocking.**

The runbook’s manual **Manage app → Reboot** fallback was not used. Every tested deployment propagated automatically. Criterion 7 requires a successful redeploy, not demonstration of every contingency branch, so the untested fallback does not block submission 2 approval.

## Mutable external-state boundary

**UNVERIFIABLE — Non-blocking.**

The evidence binds the deployment sequence and repository state at its recorded timestamps. From the package alone I cannot guarantee that, at the present instant:

* `origin/staging` still equals `500613ec81dd972a3ad53d8f4ec618665ad90c5f`;
* the deployed page still renders that SHA;
* the app’s privacy setting is unchanged;
* Community Cloud has not restarted, slept or redeployed the app;
* the cited CI and branch-protection state remains unchanged.

Approval applies to the submitted sequence and named head of record.

**GMR-004 submission 2 is approved at head of record `500613ec81dd972a3ad53d8f4ec618665ad90c5f`. The separate GMR-004 closeout PR may now be built, and its completed record must quote both submission verdicts and their respective approved SHAs/head of record.**
````
